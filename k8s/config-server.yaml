apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  labels:
    app: config-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config-server
  template:
    metadata:
      labels:
        app: config-server
    spec:
      volumes:
        - name: config-files-storage
          hostPath:
            # directory location on host
            path: /root/git/spring-cloud-config-demo
            # this field is optional
            type: Directory
      imagePullSecrets:
        - name: aliyun-docker-regcred
      nodeSelector:
        config.files.prepared: true
      containers:
        - name: config-server
          image: "registry.cn-hangzhou.aliyuncs.com/wsy-docker/config-server:V1"
          ports:
            - containerPort: 4444
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - mountPath: "/usr/share/config-files"
              name: config-files-storage

