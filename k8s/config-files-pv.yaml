apiVersion: v1
kind: PersistentVolume
metadata:
  name: config-files
  labels:
      type: local
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 500m
  hostPath:
    path: "/root/git/spring-cloud-config-demo"
