kind: Service
apiVersion: v1
metadata:
  name: config-server
  labels:
    app: config-server
spec:
  selector:
    app: config-server
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4444
  type: NodePort
